<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.DepartmentMapper">

    <sql id="Base_Column_List">
        id,name,description
    </sql>

    <select id="selectAll" resultType="com.example.entity.Department">
        select
            department.*,hospital.hospital_name as hospitalName
        from department
        left join hospital on department.hospital_id = hospital.id
        <where>
            <if test="id != null"> and id= #{id}</if>
            <if test="name != null"> and name like concat('%', #{name}, '%')</if>
            <if test="description != null"> and description= #{description}</if>
            <if test="hospitalId != null"> and hospital_id= #{hospitalId}</if>
        </where>
        order by id desc
    </select>

    <select id="selectById" resultType="com.example.entity.Department">
        select
        <include refid="Base_Column_List" />
        from department
        where id = #{id}
    </select>
    <select id="selectByH" resultType="com.example.entity.Department">
        select * from department where hospital_id =#{H_id}
    </select>

    <delete id="deleteById">
        delete from department
        where  id = #{id}
    </delete>

    <insert id="insert" parameterType="com.example.entity.Department" useGeneratedKeys="true">
        insert into department
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="id != null">id,</if>
                <if test="name != null">name,</if>
                <if test="description != null">description,</if>
                <if test="hospitalId != null">hospital_id,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="id != null">#{id},</if>
                <if test="name != null">#{name},</if>
                <if test="description != null">#{description},</if>
                <if test="hospitalId != null">#{hospitalId},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.example.entity.Department">
        update department
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="hospitalId != null">
                hospital_id = #{hospitalId},
            </if>
        </set>
        where id = #{id} 
    </update>

</mapper>